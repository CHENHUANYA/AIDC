# Delivery Risk Status

Updated: 2026-08-21

This file tracks the delivery-closeout risks that still need explicit evidence
before sharing, recording, or deploying Alarm RAG.

## Current Status

| Area | Status | Evidence / Next Action |
|---|---|---|
| Core technical-debt reduction | Passed locally 2026-08-21 | 764 Python tests plus 30 subtests and 17 JavaScript tests passed; Ruff and mypy passed. Total branch coverage is 91%. Account policy, local session persistence/cookies/password helpers, and login-throttle state now live in dedicated services; work-order lifecycle/closure/knowledge-candidate/field-permission rules and spreadsheet conversion now live outside `work_orders.py`; Ollama/School HTTP adapters and SSE stream assembly now live outside the chat route. Compatibility wrappers preserve the existing route and test interfaces. `auth.py` has fallen from 688 to 559 statements, `work_orders.py` from 924 to 772, and `routes/chat_lookup_routes.py` from 405 to 328; their branch coverage is 83%, 82%, and 95% respectively. Focused branch coverage is 97% for `db/session.py`, 98% for `repositories/rag_answers.py`, 93% for `ingest.py`, 91% for `rag_engine.py`, and 92% for `repositories/postgres_workflow.py`. `ingest.py` exposes a testable `main(argv)` while preserving CLI exit behavior. Remaining maintainability work is the larger work-order CRUD/knowledge-review split and decomposition of the non-streaming chat handler; neither is coupled to a coverage shortfall. |
| Stats / static-reference optimization | Passed locally 2026-08-21 | 722 Python tests plus 30 subtests and 17 JavaScript tests passed; Ruff and mypy passed across 47 source files. Total branch coverage is 86%, while `routes/stats_routes.py` and `routes/static_reference_routes.py` both reached 100% focused branch coverage. Alarm clearing now updates in-memory history only after PostgreSQL or JSON persistence succeeds, feedback/query/statistics storage failures return explicit 503 responses, malformed JSONL values and invalid elapsed times are ignored, and blocking persistence reads/writes run in the thread pool. Static HTML files resolve from the project root instead of the process working directory. JSONL append now creates the actual target directory and readers reject non-object values. |
| Work-order import / persistence optimization | Passed locally 2026-08-18; committed and pushed in `5aaa0e0` | 692 Python tests plus 30 subtests and 17 JavaScript tests passed; Ruff and mypy passed across 47 source files. Total branch coverage is 85%, and `work_orders.py` increased from 69% to 81%. Excel rows are now built and closure-validated before a single persistence operation, so invalid completed/verified rows cannot leave pending PostgreSQL artifacts and failed candidate writes do not inflate import counts. Missing storage directories are created safely, legacy `.xls` is rejected before staging, positional errors report the correct row, and workbooks close on every exit path. Local work-order JSON uses a reusable fsynced temporary-file plus atomic-replace service; malformed roots, invalid records, corrupt archive files, disappearing archive files, and archive directory read failures are covered. |
| Chat lookup resilience | Passed locally 2026-08-18; committed and pushed in `5aaa0e0` | 680 Python tests plus 30 subtests and 17 JavaScript tests passed; Ruff and mypy passed. Total branch coverage is 83%, and `routes/chat_lookup_routes.py` increased from 64% to 96%. Provider timeouts produce an explicit unavailable snapshot, School API fallback remains request-local, no-citation answers complete normally, and query/error/snapshot persistence failures no longer turn a usable answer into an HTTP or SSE failure. Both task cancellation and consumer-driven async-generator closure record an interrupted outcome and exit without persisting a partial answer; a provider failure after partial output instead completes with a clearly classified fallback snapshot and terminal SSE events. Ollama and School API HTTP adapters now have payload, streaming-event, invalid-JSON, provider-error, URL, and authorization-header runtime coverage. |
| Storage / auth optimization | Passed locally 2026-08-18; committed and pushed in `5aaa0e0` | 658 Python tests plus 30 subtests and 17 JavaScript tests passed; Ruff and mypy passed. Total branch coverage is 81%, exceeding the original 78–80% target; `auth.py` is 83% and `storage.py` is 99%. Local manifest writes now use a flushed and fsynced temporary file plus atomic replace, retain the prior valid snapshot, recover from a corrupt or missing primary manifest, normalize supported legacy shapes, and reject invalid document structures without publishing a partial file. Runtime auth tests now cover account creation/update validation, last-admin and self-demotion protection, session listing/revocation including ambiguous prefixes, bootstrap stores, corrupt JSON behavior, and legacy plain-token migration. |
| RAG answer traceability | Implemented; PostgreSQL live verified | Immutable snapshots include `answer_state=complete/fallback/unavailable`, citations, provider/model, versions, latency, actor, and timestamp. Supervisor/Admin share a read-only Answer Trace panel. PostgreSQL migration head is `20260713_0006`. |
| Reranker runtime | Live verified with multilingual safeguard | Fixed duplicate `local_files_only` loading arguments. Live gate recorded `loaded=true`, `active=true`, 8 inference calls, and no runtime error. Chinese/mixed queries retain RRF ordering because the bundled MS MARCO cross-encoder is English-only. |
| Ollama response latency | Improved locally; still needs target SLO | Warm live chat measured 24.961 seconds versus the prior ~49-second observation after context/output caps and keep-alive configuration. This is still hardware/model dependent and must be measured on the pilot host. |
| Answer quality evaluation | Engineering v2 gate implemented | Retrieval v2 contains 45 cases (15 each for 808D/840D/840D sl) with global and per-collection gates. Answer-quality v2 adds adversarial citation/source/parameter/safety detection. It does not replace technician review. |
| Local restart recovery | Verified 2026-07-13 | Latest controlled app restart recovered in 19.297 seconds and Qdrant in 20.311 seconds. Preflight and post-restart checks matched all 2,075 expected `808d` points; health, authentication, lookup, and structured retrieval passed. Target-host restart evidence remains required. |
| Bugfix / code-quality re-review | Passed locally 2026-08-18 | 601 Python tests plus 30 subtests and 17 JavaScript tests passed. Ruff passed, and mypy checks 46 source files including `app_context.py`, `rag_engine.py`, `ingest.py`, `storage.py`, and `services/`. Total branch coverage is 77% and the CI floor is now 75%; `services/postgres_workflow.py`, `repositories/postgres_content.py`, and `vector_store.py` have 100% branch coverage, `routes/ingest_routes.py` is 97%, `issues.py` is 91%, `ingest.py` is 74%, `work_orders.py` is 69%, `repositories/postgres_workflow.py` is 76%, `auth.py` is 61%, and `repositories/postgres_auth.py` is 86%. Password resets now carry the displayed user version and enforce a second optimistic-lock check at persistence time, preventing concurrent role or activation changes from being overwritten before sessions are revoked. The isolated Admin browser E2E verifies visible success, stale-version conflict, and persistence-failure messages; its browser, unexpected HTTP, third-party request, and eight-page accessibility error counts are all zero. Work orders now enforce explicit transitions across pending, assigned, in-progress, completed, verified, and cancelled states; cancelled records are terminal, and JSON bidirectional issue/work-order synchronization restores both snapshots after a partial failure while PostgreSQL lets the enclosing transaction roll back. Soft-deleting a linked work order now unlinks and reopens the active issue, restores both JSON records if unlinking fails, and prevents PostgreSQL issue reads from exposing deleted work-order links. Knowledge review requests carry optimistic versions, PostgreSQL saves only the reviewed record, duplicate decisions are audited, and ingestion failures remain visible for retry. PostgreSQL cursor, role, status, and soft-deleted-link branches have repository-level coverage. Vector adapters reject mismatched batch inputs before contacting Chroma or Qdrant, and issue API tests lock down creation, optimistic updates, reopen/verification rules, history, cursor pagination, and work-order escalation. Document deletion restores the prior in-memory index when metadata optimistic locking loses a race. Historical JSON error envelopes receive 400/401/403/404/409/410/503 at the HTTP boundary. All JSON endpoint families publish explicit OpenAPI success/error schemas, enforced by a global contract test; chat documents both JSON and `text/event-stream` responses. |
| Native Ollama streaming | Verified locally 2026-07-13 | Live gate observed 30 incremental content events with one answer ID, first content at 2.875s and total 7.967s. Completed stream snapshot persisted with provider/citation metadata; interrupted streams are not persisted. |
| School API success path | Excluded from current engineering acceptance | School-owned credentials/network behavior are not a release gate for this implementation pass. No School API success claim is made. |
| n8n true end-to-end | Verified locally | n8n CLI import/list/execute completed. Latest one-off workflow execution returned `status=ok`, `issue_id=ISS-20260610-5b29c4`, `work_order.id=6c7f24b1`. |
| Documentation commands | Mostly verified | `standalone_acceptance.py`, smoke, regression, role-console, n8n workflow, backup verify/restore-smoke, runtime soak, browser E2E, and production boundary checks have been run locally. Destructive restore/secret rotation commands remain operator-confirmed only. |
| Production secrets / rotation | Tooling ready, not executed | `.env` is ignored by Git and Docker. Run `python scripts/bootstrap_env.py --rotate-secrets --reset-bootstrap-passwords --show-admin-password` only during an approved pilot-host rotation window, then recreate app/n8n containers and re-import/update workflows. |
| Malicious file safety | Improved and tested | PDF upload now checks size, `%PDF` magic, readable structure, and page count. XLSX upload checks size, zip structure, entry count, uncompressed size, `sharedStrings.xml` size, and compression ratio before `openpyxl`. |
| Long-running soak / restart recovery | Local PostgreSQL RC passed 2026-07-13 | Functional soak ran 14,404.094s with 1,167 checks and zero failures. PostgreSQL load ran 14,402.031s with 27,717 requests, zero failures, 1.925 RPS against a 2.0 target, zero residue/orphans, and concurrency PASS. App/Qdrant recovery passed in 88.452s / 59.750s. Target-host evidence remains separate. |
| UI copy and responsive acceptance | Browser evidence generated | `python scripts/browser_e2e_responsive.py` passed locally after current Admin/Supervisor tab updates. Answer Trace modal interactions now cover Supervisor/Admin open, content, button/backdrop close, desktop bounds, and mobile citation scrolling. Report status is `ok`, with screenshots under `tests_tmp/browser_e2e/screenshots/`, 0 browser errors, 0 HTTP errors, and 0 layout failures. |
| Production TLS / reverse proxy | Check tooling ready; needs real URL | Run `python scripts/production_boundary_check.py --base-url https://alarm-rag.example.com --origin https://alarm-rag.example.com --require-hsts` after proxy/TLS is configured. Local same-origin/SSE boundary check passes. |
| Qdrant authenticated transport | Enforced in code and preflight | Compose/loopback HTTP is allowed only for the explicit `QDRANT_INSECURE_TRUSTED_HOSTS` allowlist. Remote hosts require `QDRANT_HTTPS=true`; an approved private-network exception is explicit and reported as a preflight warning. Local tests no longer emit the qdrant-client insecure API-key warning. |
| Pull-request CI | Trigger configured; remote run pending | `.github/workflows/ci.yml` runs on `pull_request` and gates JavaScript, Python 3.11/3.12 on Linux and Windows, Ruff, mypy, answer quality, dependency audit, migrations, tests/coverage, image builds, and Compose validation. Open a PR to capture remote check evidence. |

## Final Handoff Gate

Before external handoff, confirm:

- Rotate generated secrets during an approved pilot-host maintenance window.
- Re-import or update n8n workflows after token rotation.
- Run `preflight_check.py`, `standalone_acceptance.py`, `rag_runtime_check.py`, `runtime_soak.py`, `browser_e2e_responsive.py`, and `production_boundary_check.py` against the target environment.
- Keep runtime directories (`alarm_db/`, `backups/`, `data/`, `hf_cache/`, `n8n_data/`, `qdrant_data/`, `tests_tmp/`) out of Git and Docker build context.
- For a no-vendor local package, use `docs/reports/LOCAL_HANDOFF_MANIFEST_2026-06-24.md` and do not claim real plant integration until the external blockers above are cleared.
- School API remains outside this pass and must not block the pilot-host TLS, secret rotation, or soak gates.
